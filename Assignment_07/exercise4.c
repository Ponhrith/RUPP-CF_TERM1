#include <stdio.h>
int main(){
    int number;
    printf("Enter the number:");
    scanf("%d", &number);
    if (number >= 20) { 
        printf("Too high");
    }   else {
        printf("Thank you!");
    }

return 0;

}
